using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CamParameters : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //FPS
        Application.targetFrameRate = 60;
    }
}
